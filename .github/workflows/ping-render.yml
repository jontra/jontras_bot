name: Ping Render Bot

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake Render webhook
        run: curl -sS -X POST "https://significative-amelie-hyperfastidious.ngrok-free.dev/digest"
